# BankingApp
This repo is for creating a banking application.

## update 
update readme


create repo on github bankingapp
put description
public visibility
add readme
main branch is used to host final copy of your code or code going for release
update readme and commit changes
in terminal in ubuntu
mkdir mybankingapp
cd mybankingapp
ls -la
sudo git init
ls -la
sudo git remote add origin https://github.com/AdithyaRaoK14/BankingApp
sudo git pull origin main
ls
sudo git branch
sudo git branch -r
sudo git branch -a
sudo git branch main
sudo git branch
sudo git checkout main
ls
touch index.php
(echo, cat, tee command)- write into files. 
cat >> index.php
welcome to msis banking app
ctrl+c
cat index.php
sudo git status
sudo git add index.php 
sudo git status
cat >> index.php 
updated banking app
ctrl+c
cat index.php
sudo git status
sudo git commit index.php -m "first version of my index page"
go to github, settings, developers settings, personal access token, token classic, gen new token, classic , give it a name and check all boxes, generate and copy token, 
in terminal enter github repo name and token
touch about.php contact.php
ls 
cat >> about.php
cat >> contact.php
sudo git add -A
sudo git status
sudo git commit -a -m "added about and contact page"
sudo git log
sudo git log --oneline
sudo git log --oneline --graph
sudo git push origin main 
sudo git branch
ls
sudo git checkout -b fd
sudo git branch
ls
touch fd.php
cat >> fd.php
this is my fd page
ctrl +c
ls
sudo git add fd.php
sudo git commit fd.php -m "this is a feature called fd" 
ls
sudo git checkout main
ls
sudo git merge fd
ls
sudo git checkout -b rd
sudo git branch
ls
touch rd.php
cat >> rd.php
this is rd file of banking app
ctrl+c
sudo git add rd.php
sudo git commit rd.php -m "this is a rd feature"
sudo git push origin rd
in github click on compare changes, write desc, clcik on the buttons and write comments, 
